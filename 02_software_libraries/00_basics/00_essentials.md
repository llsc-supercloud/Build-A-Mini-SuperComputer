# A list of essential build tools and libraries.

1. GCC, make, C library 
2. Cmake
3. OpenBLAS

To install, you will need to be root. Do this installation on all nodes.

One line command to install GCC, cmake and openblas.

`apt install -y build-essential cmake libopenblas-dev`
